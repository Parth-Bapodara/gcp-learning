name: invoice_pipeline
version: "1.0.0"
config-version: 2

profile: invoice_pipeline

model-paths: ["models"]

models:
  invoice_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: view
